create_ical <- function(dataTable, eventSummary, rowNumber) {
  library(calendar)
  
  s = as.POSIXct(dataTable[rowNumber, 1])
  e = s + 60^2 * 24
  event = ic_event(start = s, end = e , summary = eventSummary)  
  class(event)
  
  ic_character(event)
  

}
